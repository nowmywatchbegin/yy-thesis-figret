# FIGRET 应用于 LEO 卫星网络动态拓扑的实验总结

---

## 一、项目背景与目标

### 1.1 研究对象

- **FIGRET**（SIGCOMM 2024）：一种细粒度鲁棒性增强的流量工程算法，利用深度学习和突发感知损失函数，为数据中心/WAN 网络生成高质量路由方案
- **LEO 卫星网络**：96 颗卫星组成的 Walker 星座（8 轨道面 × 12 卫星），189 个时间片（每片间隔 30 秒），192 条星间链路（ISL）
- **核心问题**：FIGRET 原为静态拓扑设计，能否直接应用于拓扑动态变化的 LEO 卫星网络？

### 1.2 实验目标

1. 证明 FIGRET 直接应用于 LEO 动态拓扑会失效
2. 提出改进方案（路径存活率惩罚）
3. 验证改进效果

---

## 二、数据预处理

### 2.1 原始数据

- 师兄提供的 189 个 txt 文件（`Data/leo_raw/`）
- 每个文件包含：`[METADATA]`（时间戳）、`[NODES]`（96 颗卫星的 ID、经纬度、高度）、`[LINKS]`（192 条 ISL 链路）
- 初始发现：189 个时间片的 ISL 连接关系完全相同，只有卫星坐标在变

### 2.2 动态拓扑生成（距离筛选）

利用卫星坐标计算每条 ISL 链路的 3D 空间距离（半正矢公式 + 高度修正）。

**距离阈值 4300 km**：超过此距离视为链路断开。理由：
- 全部链路距离范围 1051~4836 km
- Starlink 激光 ISL 实测最远 5400 km（SPIE Photonics West 2024）
- 4300 km 使每片存活 176~186 条链路，102/189 片与片 0 的链路集合不同

**产出**：189 个 NetworkX 格式的 JSON 拓扑文件（`Data/leo/topologies/step_XXXX.json`）

### 2.3 链路容量

**ISL 容量设为 1 Gbps**，参考 Starlink 激光 ISL（25~200 Gbps）取保守值。

### 2.4 候选路径生成

从全图（192 条链路）使用 K=3 最短路径算法，为 9120 对源目的卫星生成候选路径。

| 指标 | 数值 |
|------|------|
| S-D 对 | 96 × 95 = 9120 |
| 候选路径总数 | 24866 条 |
| 每对路径数 | 1~3 条（平均 2.73） |

产出：`Data/leo/tunnels_full.txt`

### 2.5 模拟流量生成

从一个 4 节点真实网络数据（Facebook_pod_a，7499 时刻）中提取流量的统计规律，为 LEO 网络生成模拟流量。

**Facebook 数据统计特征**：
- 均值范围：9900~58900 bps
- CV（变异系数 = 标准差÷均值）范围：0.23~0.58
- 突发概率（超过 2 倍均值）：0.2%~4.6%
- 相邻时刻自相关：-0.001~0.158（基本独立）

**生成方法**：
1. 对 LEO 的 9120 对 S-D，每对随机抽取一组 Facebook 的统计参数（均值 + 标准差）
2. 对每个时刻：`流量 = max(0, 均值 + 正态噪声 × 标准差)`，按突发概率随机触发尖峰
3. 流量值乘以 70 倍，匹配 1 Gbps 的 ISL 容量

**产出**：2400 行训练流量 + 600 行测试流量（`Data/leo/train/0.hist`, `test/0.hist`）

### 2.6 路径存活率统计

**路径存活率** = 路径上所有链路存活率的最小值（木桶原理：最脆弱的链路决定整条路径的稳定性）。

- 训练集（片 0~49）：路径存活率范围 0.46~1.0，均值 0.87
- 每片有效路径数：13270~22972 条（总 24866），平均 17797 条

---

## 三、Phase 1：基准测试

### 3.1 实验设计

- **训练**：在时间片 i 的拓扑上训练 FIGRET（30 epoch，hist_len=12）
- **测试**：在后续时间片上使用同一模型测试 MLU
- **对比基线**：ECMP（均分）、SPF（最短路径优先）、DW（距离加权）
- **评估指标**：MLU（最大链路利用率），越低越好

### 3.2 实验结果

**模型 0000（在片 0 训练，测试片 1~50）**：
- 片 1~10：FIGRET 完胜全部基线
- **片 38：首次被反超**（SPF + DW 双杀）
- **片 49：彻底失效**（被 ECMP + SPF + DW 三杀）

**模型 0001（在片 1 训练，测试片 2~52）**：
- **片 38：首次失效**（复现了一致的失效模式）

### 3.3 关键结论

FIGRET 在拓扑变化累积约 38 个时间片后，性能系统性地被简单基线算法反超，证明直接应用不可行。

---

## 四、Phase 2：改进 FIGRET

### 4.1 改进方法

在 FIGRET 的损失函数中新增**路径存活率惩罚项**：

```
原始 loss = max_cong + α × 敏感度惩罚

改进 loss = max_cong + α × 敏感度惩罚 + β × 路径存活率惩罚
                                             ↑
                         β × mean(split_ratio × (1 - path_survival))
```

**含义**：存活率低的路径如果被分配较高比例的流量，会受到更大的惩罚。这迫使模型在训练时主动避开不稳定的路径。

### 4.2 代码修改

| 文件 | 改动内容 |
|------|---------|
| `figret_helper.py` | 新增 `--beta` 命令行参数 |
| `figret_env.py` | `load_path_survival()` 加载预计算的路径存活率 |
| `figret_model.py` | `loss()` 函数新增第三项路径存活率惩罚 |

改动总量约 30 行。

### 4.3 实验设计

- **训练拓扑**：全图（192 条链路，所有路径有效）
- **训练参数**：α=0.03，β=0.1，15 epoch
- **路径存活率来源**：训练集片 0~49
- **测试范围**：片 50~188（纯测试集）

### 4.4 实验结果

| 指标 | 旧 FIGRET | 新 FIGRET（β=0.1） |
|------|----------|------------------|
| 平均 MLU（片 50~188） | 1.1518 | 1.1382 |
| 首次失效 | 片 50 | 片 50 |
| 失效片数 | 58/139 | 58/139 |

MLU 小幅下降（~1.2%），但失效点未推迟。

### 4.5 失效分析

在片 58 发现 8 条链路在训练集中存活率 100%（50/50），到片 58 突然断开：

```
(15,26) (21,32) (27,38) (33,44) (50,61) (56,67) (85,3) (91,9)
```

这些全是跨轨道面 ISL。训练集覆盖有限（50 片），某些链路在训练集内从未断开，β 惩罚无法预警（存活率=1.0，惩罚项=0）。

### 4.6 关键结论

路径存活率惩罚有微弱的正向效果（MLU 降低 1.2%），但无法显著推迟失效点。原因：
1. 训练集覆盖不全——50 片不足以反映所有链路的长期存活模式
2. 纯损失函数惩罚无法替代真实的多拓扑体验——模型从未经历链路断裂
3. 论文的递进结论：Phase 1 证明失效 → Phase 2 证明简单惩罚不够 → 未来需要真正的多拓扑在线训练

---

## 五、最终结论

1. **FIGRET 直接应用于 LEO 网络会失效**：约 38 个时间片后被简单基线反超
2. **路径存活率惩罚有正向效果但不显著**：MLU 降低 1.2%，失效点未推迟
3. **失效根因**：训练集有限导致某些链路在训练中"伪装"稳定，测试时突然断裂
4. **未来方向**：更大的训练集、真正的多拓扑联合训练

---

## 六、关键参数汇总

| 参数 | 值 | 说明 |
|------|-----|------|
| 卫星数 | 96 | 8 轨道面 × 12 卫星 |
| 时间片数 | 189 | 间隔 30 秒 |
| ISL 链路数 | 192 | 经 4300km 阈值筛选后 176~186 条 |
| ISL 容量 | 1 Gbps | 参考 Starlink 取保守值 |
| S-D 对 | 9120 | 96 × 95 |
| 候选路径 | 24866 条 | K=3 最短路径 |
| 训练样本 | 2400 行 | 模拟流量 |
| hist_len | 12 | 看过去 12 个时刻 |
| 模型输入维度 | 109440 | 12 × 9120 |
| 模型输出维度 | 24866 | 路径总数 |
| α | 0.03 | 敏感度惩罚权重 |
| β | 0.1 | 路径存活率惩罚权重 |

---

## 七、产出文件清单

### 预处理脚本
- `preprocess_0a_topology.py` — 距离计算 + 拓扑 JSON 生成
- `preprocess_0b_paths.py` — K 最短路径生成
- `preprocess_0c_traffic.py` — 模拟流量生成

### Phase 1 脚本
- `phase1_train.py` — 批量训练模型
- `phase1_test_model.py` — 通用跨拓扑测试 + 画图
- `phase1_test.py` — 基线算法计算

### Phase 2 脚本
- `phase2_train_final.py` — 多拓扑训练（含 β 惩罚）
- `phase2_quick_test.py` — 快测脚本
- `phase2_full_plot.py` — 全 189 片对比图
- `phase2_compare.py` — 新旧 FIGRET 对比
- `phase2_grid.py` — β 网格搜索

### 结果图
- `Result/leo/baselines_all_slices.png` — 基线全视图
- `Result/leo/convergence_model_trained_on_slice0.png` — 收敛曲线
- `Result/leo/figret_0000_trained_on_slice0_tested_on_1to50.png` — Phase 1 核心结果
- `Result/leo/phase2_full_comparison.png` — Phase 2 全 189 片对比
- `Result/leo/path_survival_by_slice.png` — 每片存活路径数

### 数据文件
- `Data/leo/leo.json` — 训练拓扑
- `Data/leo/full_leo.json` — 全图拓扑
- `Data/leo/tunnels_full.txt` — 候选路径
- `Data/leo/topologies/step_XXXX.json` — 189 个时间片拓扑
- `Data/leo/link_survival.json` — 链路存活率
- `Data/leo/path_survival_train.json` — 路径存活率（训练集）

### 修改的源文件
- `src/figret_env.py` — 新增 valid_path_mask + path_survival 加载
- `src/figret_model.py` — loss 函数加 β 惩罚 + 无效路径归零
- `figret_helper.py` — 新增 --beta 参数
